import { Josefin_Slab, Roboto_Mono, Tajawal, Cormorant } from 'next/font/google';

export const josefin_slab = Josefin_Slab({ subsets: ['latin'] });
export const roboto_mono = Roboto_Mono({ subsets: ['latin'] });
export const cormorant = Cormorant({ subsets: ['latin'] })
